% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/row_id_by_pattern.R
\name{row_id_by_pattern}
\alias{row_id_by_pattern}
\title{Row ids by cell pattern}
\usage{
row_id_by_pattern(
  DT,
  incl_cols = seq_along(DT),
  excl_cols = NULL,
  na_is = 0L,
  magnitude = FALSE,
  max_patterns = .Machine$integer.max,
  nThread = getOption("hutilscpp.nThread", 1L)
)
}
\arguments{
\item{DT}{A \code{data.frame} (typically a \code{data.table}). It is not modified.}

\item{incl_cols, excl_cols}{Which columns of \code{DT} contribute to the pattern.
Each is either a vector of column positions or a character vector of
column names. The columns used are those in \code{incl_cols} that are not in
\code{excl_cols}, so \code{excl_cols} has priority. By default every column is
used (\code{incl_cols = seq_along(DT)}, \code{excl_cols = NULL}). Column selection
reads the existing columns in place; it does not copy them or build a
new table, so it is cheap on large tables. If no column remains, every
row shares the pattern and the result is all \code{1L}.}

\item{na_is}{\code{0L} or \code{1L}. The pattern value assigned to \code{NA} and \code{NaN} in
integer, logical, and double columns.}

\item{magnitude}{\code{TRUE} or \code{FALSE}. If \code{FALSE} (the default), integer and
double columns are summarised as zero versus nonzero. If \code{TRUE}, they are
summarised by sign and \code{floor(log2(abs(x)))}, so values of the same sign
and binary order of magnitude share a pattern. See Details.}

\item{max_patterns}{A positive whole number. Only the \code{max_patterns} most
frequent patterns receive an id; rows belonging to any other pattern
are \code{NA_integer_}. The default (\code{.Machine$integer.max}) is the largest
number of ids an integer vector can hold.}

\item{nThread}{Number of threads to use.}
}
\value{
An integer vector with one element per row of \code{DT}. Ids are dense
(\code{1L} to the number of retained patterns), ordered by decreasing
frequency, with ties broken by first appearance in \code{DT}.
}
\description{
Assign every row of a data.frame an integer id such that two
rows share an id if and only if they have the same \emph{pattern} in every
column, where the pattern of a cell is a cheap summary of its value rather
than the value itself. Ids are ranked by frequency: \code{1L} is the most common
pattern.
}
\details{
The pattern of a cell depends on the column type:
\itemize{
\item \strong{integer, double, logical}: \code{1L} if nonzero, \code{0L} if zero.
\code{NA} and \code{NaN} take the value \code{na_is}. \code{-0} counts as zero.
\item \strong{raw}: \code{1L} if nonzero, \code{0L} if zero.
\item \strong{factor}: the integer code, so rows are grouped by level. \code{NA} is its own
pattern.
\item \strong{character}: the string itself. \code{NA_character_} is its own pattern.
Strings are compared by identity in R's global string cache, so two
strings that are equal only after translation between encodings are
treated as distinct.
\item Other classed columns backed by integer or double storage (\code{Date},
\code{IDate}, \code{POSIXct}, ...) follow their storage type. Columns of class
\code{integer64} or \code{nanotime} are not supported, since their \code{NA} is stored
as a bit pattern that would read as zero.
}

With \code{magnitude = TRUE} the pattern of an integer or double is one of:
zero; a separate category for \code{NA}/\code{NaN} when \code{na_is = 1L} (when
\code{na_is = 0L} they join the zero category); otherwise the pair
(\code{sign(x)}, \code{floor(log2(abs(x)))}), computed exactly from the binary
representation (subnormal doubles included). \code{Inf} and \code{-Inf} are their own
categories. Logical and raw columns are unaffected by \code{magnitude}.

Rows are compared through a 128-bit hash of their patterns rather than
pattern-by-pattern, so distinct patterns could in principle collide. The
probability is below \code{1e-20} for any table that fits in memory.

Every column must be an atomic vector with one element per row. Matrix
columns (for example from \code{I(matrix(...))}) and nested data.frame columns
are rejected rather than silently misaligned.

Columns that are ALTREP (for example \code{1:n}) are expanded before hashing.
}
\examples{
DT <- data.frame(a = c(0, 1, 2, 0), b = c("x", "y", "y", "x"))
row_id_by_pattern(DT)
row_id_by_pattern(DT, max_patterns = 1L)
row_id_by_pattern(DT, incl_cols = "a")
row_id_by_pattern(DT, excl_cols = 1L)
row_id_by_pattern(data.frame(x = c(1, 2, 3, 4, -1, 0)), magnitude = TRUE)

}
